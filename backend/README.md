# Meachine learning project examples

## venv

```shell
source venv/bin/activate
```

## install

```shell
python -m pip install -r requirements.txt
python -m pip install -r requirements-test.txt
```
